# Harness output: regenerated by `python3 tests/renderer-parity/harness.py`.
# `fonts/` holds copies of system font files assembled at run time for
# render-card-pdf.py; they are not ours to commit.
out/
