# Public account-deletion guide required by app stores.
# Install inside the cardify.om server block before the tenant catch-all.
rewrite ^/delete-account/?$ /delete-account.php last;
rewrite ^/ar/delete-account/?$ /delete-account.php?lang=ar last;
